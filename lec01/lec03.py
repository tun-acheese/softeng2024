from email.contentmanager import set_message_content
    if n < 2:
        return False

    for i in range(2,n):
        if n % i == 0:
            reutrn False

            return True
        if is_prime(n):


def is_prime(n: int) -> bool: 1uset_message_content()


def main(): 1 usage
    n = 7

    if is_prime()